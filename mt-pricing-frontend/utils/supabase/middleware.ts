/**
 * Re-export del middleware Supabase.
 *
 * El código canónico vive en `@/lib/supabase/middleware`. Este archivo existe
 * para matchear el patrón oficial de Supabase docs (`utils/supabase/middleware.ts`).
 */
export { updateSession } from "@/lib/supabase/middleware";
